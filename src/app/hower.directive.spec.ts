import { HowerDirective } from './hower.directive';

describe('HowerDirective', () => {
  it('should create an instance', () => {
    const directive = new HowerDirective();
    expect(directive).toBeTruthy();
  });
});
